{% for marque in marques %}
    {% if marque.front == 1 %}
        <a href="javascript:void(0)" class="brand">
            <img src="{{ asset('uploads/media/marques/') ~ marque.files }}" alt="{{ marque.name }}">
        </a>
    {% endif %}
{% endfor %}